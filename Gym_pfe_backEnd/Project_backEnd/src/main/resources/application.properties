spring.application.name=Project_backEnd

# ?? Base de données ??
spring.datasource.url=jdbc:postgresql://localhost:5432/smart_gym_db
spring.datasource.username=postgres
spring.datasource.password=123456

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ?? AI Service ??
ai.service.url=http://127.0.0.1:8000
server.address=0.0.0.0
server.port=8080


# ?? JWT ??
jwt.secret=SmartGym2024_SuperSecretKey_Change_Before_Deploy_32chars!

# ?? Upload fichiers (optionnel) ??
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB